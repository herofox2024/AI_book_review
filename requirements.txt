# 内核依赖（解析 + 摘要 + 生成）
python-docx>=1.1.0
ebooklib>=0.18
beautifulsoup4>=4.12
lxml>=5.0
# Optional MOBI support; use Calibre fallback when mobi is unavailable.
openai>=1.40

# GUI（P5，已完成）
PySide6>=6.6

# 打包阶段（P6）再安装
# pyinstaller>=6.0
